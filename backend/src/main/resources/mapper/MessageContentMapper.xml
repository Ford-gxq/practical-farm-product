<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.MessageContentMapper">
    <sql id="messageColumns">
        id, name, phone, content, source, status, admin_remark, created_at, updated_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message_contents(name, phone, content, source, status)
        VALUES (#{name}, #{phone}, #{content}, #{source}, #{status})
    </insert>

    <select id="selectPage" resultType="MessageContent">
        SELECT <include refid="messageColumns"/>
        FROM message_contents
        WHERE 1 = 1
        <if test="status != null">AND status = #{status}</if>
        <if test="keyword != null and keyword != ''">
            AND (
                name LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
                OR content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(1)
        FROM message_contents
        WHERE 1 = 1
        <if test="status != null">AND status = #{status}</if>
        <if test="keyword != null and keyword != ''">
            AND (
                name LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%')
                OR content LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
    </select>

    <select id="selectById" resultType="MessageContent">
        SELECT <include refid="messageColumns"/>
        FROM message_contents
        WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE message_contents
        SET status = #{status},
            admin_remark = #{adminRemark}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM message_contents WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="long">SELECT COUNT(1) FROM message_contents</select>
    <select id="countUnhandled" resultType="long">SELECT COUNT(1) FROM message_contents WHERE status = 0</select>
</mapper>
